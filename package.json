{
  "name": "evenx-website",
  "version": "1.0.0",
  "description": "EvenX - Split Expenses Smartly - Official Website",
  "private": true,
  "engines": {
    "node": ">=18.14.0",
    "yarn": ">=1.22.0"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.3.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "type-check": "tsc --noEmit",
    "build:local": "REACT_APP_API_URL=http://localhost:8080 yarn build",
    "build:dev": "REACT_APP_API_URL=http://35.244.150.211 yarn build",
    "build:prod": "REACT_APP_API_URL=https://api.evenx.io yarn build",
    "docker:build:local": "./scripts/build-and-push.sh local",
    "docker:build:dev": "./scripts/build-and-push.sh dev",
    "docker:build:prod": "./scripts/build-and-push.sh prod",
    "deploy:dev": "kubectl apply -f k8s/deployment-dev.yaml",
    "deploy:prod": "kubectl apply -f k8s/deployment-prod.yaml"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.9.2"
  }
}
